# QWEN V2.4 FINAL CORRECTION: MATERIAL SIGNATURE + 90-CHECK GATE

Continue as the Qwen design/implementation lead from the CURRENT v2.3 source.
This is one final bounded correction pass based on fresh independent browser QA
and original-pixel target comparison. Do not create a new concept or documents.

Edit only `index.html`, `assets/qwen-full-film.css`, and
`assets/qwen-full-film.js`. Do not touch any other route, shared analytics code,
packages, scripts, reports, Git/GitHub, production, ads, Metrika settings, Mango
or budgets. No external assets or network calls.

## Preserve all v2.3 wins

Keep exact service truth, fixed 180,000 RUB price, 3-5 days, 5-year warranty,
taxi term, question-first workflow, contact destinations, all canonical events,
UTM message variants, dialog keyboard behavior, three retained proof files,
manual-only gallery, no autoplay, and static reduced-motion/Save-Data behavior.

## Fresh independent QA: 83/90, fix all seven failures

Report: `artifacts/sol-qa-v2-3/sol-qa-v2-3-report.json`.

1. Mobile decorative clipping: at 430, 390 and 360, `.tf-field`, its SVG/group,
   paths and light rect extend beyond the physical viewport. Document width did
   not scroll, but the strict clipping gate correctly failed. Constrain every
   Tension Field element within 0..viewport width. Use `right:0`, a safe width,
   SVG `overflow:hidden`, `preserveAspectRatio="xMidYMid meet"`, a viewBox-safe
   group/clipPath and no negative/off-canvas bounds. Preserve first-viewport
   visibility and do not shrink it into an insignificant icon.
2. No-JS image integrity: the two secondary thumbnail `<img>` elements with no
   `src` have naturalWidth 0 and are broken. Give them a tiny valid inline data
   image placeholder (not a network request and not fake proof) while retaining
   their real WebP only in `data-src`. JS must replace the placeholder when the
   proof section approaches or the user navigates. No-JS must show the first real
   proof image and usable numbered secondary controls with zero broken images.
3. Privacy: add both official Yandex opt-out classes
   `ym-disable-tracklink ym-disable-clickmap` to ALL three WhatsApp anchors
   (hero, contact, dialog). Preserve their existing classes and canonical
   `reachGoal` tracking; do not change counter settings.

The next run must pass all 90 checks, including responsive accessible names,
zero initial secondary WebP ResourceTiming, three retained proof files decoded
after traversal, no-JS, reduced motion, real Save-Data, dialog, 44px targets,
message match, canonical events, QA isolation and zero Metrika requests.

## Truth-copy leftovers

4. Replace the current Russian label meaning `First step - without obligations`
   with the exact Russian meaning `First step - consultation`.
5. Replace the current dialog promise that the administrator will offer a nearby
   convenient slot with factual Russian copy meaning: the administrator will
   clarify package applicability and order details, then agree the visit time.
6. Search all metadata/body/copy-message text and keep factual neutral language:
   no premium/superlative film claim, no guaranteed outcome, no hidden-stage
   claim, no automatic defect-removal claim, no unverified care promise.

## Visual target is still not translated strongly enough

Compare directly:

- accepted desktop: `artifacts/qwen-full-film-concept-targets/desktop-tension-target-v2.png`
- accepted mobile: `artifacts/qwen-full-film-concept-targets/mobile-tension-target-v2.png`
- current v2.3: `artifacts/sol-qa-v2-3/desktop-1440x1000-hero.png` and
  `artifacts/sol-qa-v2-3/mobile-390x844-hero.png`

The current v2.3 membrane is finally large, but it renders as a pale beige blob.
It lacks the accepted target's transparent material, dual glass-like edge,
specular highlight, optical tension and bright inspection-light cut. On mobile
it reads like a low-opacity background panel behind the form, not protective
film. A one-shot light sweep ends at opacity 0, so static screenshots lose the
signature.

7. Make the code-native SVG visibly material in the final static state:
   - mostly neutral translucent film, not a flat amber fill;
   - two controlled glass-like edge highlights plus a subtle darker fold/shadow;
   - a restrained layered offset/refraction cue inside the membrane so the
     background appears optically displaced rather than merely tinted;
   - a persistent diagonal specular/inspection light cut with a soft warm halo
     and a narrow white core, visible after entrance motion finishes;
   - local soft grain/optical variation using lightweight SVG/CSS, not a flat
     card and not a full-screen raster;
   - enough contrast to read at 1440 and 390/360 while never obscuring copy or
     controls.
8. The entrance animation may briefly intensify/move the inspection light, but
   must finish at a useful persistent static state (not opacity 0). Keep it one
   shot, no infinite animation. Reduced-motion/Save-Data/no-JS must show the same
   meaningful static material signature without animation.
9. Keep the continuation into the next chapter, but reduce the empty mobile gap.
   The next heading should begin deliberately after the hero, not after a dead
   band. Do not rebuild the proven IA or contact mechanics.

Acceptance intent remains: target translation >=0.72, signature >=8/10,
premium craft >=8.2/10, with all functional gates green. Do not claim those
scores yourself; Sol will visually score the fresh render.

## Static checks before return

- `node --check assets/qwen-full-film.js`
- no blocked proof filename in source
- no `infinite` animation declaration
- no mobile negative/right-offscreen Tension Field bounds
- every `<img>` has a valid `src` in no-JS HTML
- secondary real WebP files remain only `data-src` initially
- all three WhatsApp anchors have both opt-out classes
- car input still has `ym-disable-keys`
- price, terms, destinations, UTM variants and event names unchanged

Return exact changes and unresolved browser evidence. Sol will rerun all QA,
performance, motion, provenance and visual gates.
