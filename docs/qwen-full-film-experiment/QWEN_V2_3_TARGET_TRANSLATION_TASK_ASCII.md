# QWEN V2.3 TARGET-TRANSLATION AND RELEASE-BLOCKER PASS

You remain the design and implementation lead for the current OHTAAWA full-film
landing. Continue from the CURRENT v2.2 source in this workspace. This is one
bounded implementation pass, not a new concept and not a document-only answer.

Edit only:

- `index.html`
- `assets/qwen-full-film.css`
- `assets/qwen-full-film.js`

Do not edit any other route, shared `assets/app.js`, analytics settings, Git,
GitHub, production, ads, budgets, Mango, packages, screenshots or QA reports.
Do not install anything and do not use external network assets.

## Fixed product truth and interaction contract

- Service: full transparent protective film on painted body panels.
- Fixed confirmed price: 180,000 RUB.
- Confirmed terms remain: 3-5 days, 5-year warranty, taxi at studio expense.
- Keep the current question-first contact mechanics, canonical event names,
  UTM message variants, manual-only proof gallery, dialog model and all contact
  destinations. No autoplay, fake video, fake proof, testimonials or claims.
- All content must remain usable with no JS, reduced motion and Save-Data.

## Independent owner-grade failure to fix

Accepted visual targets are:

- `artifacts/qwen-full-film-concept-targets/desktop-tension-target-v2.png`
- `artifacts/qwen-full-film-concept-targets/mobile-tension-target-v2.png`

The current source translates them at only about 0.58. The signature membrane
is a pale technical outline, disappears below the first 390/360 viewport and
creates a dead blank strip. The target is a large, material, translucent film
membrane with optical tension, a controlled light cut and premium editorial
type. Translate that target much more faithfully with code-native HTML/SVG/CSS;
do not rasterize the whole UI and do not embed the target screenshot.

Required visual changes:

1. Make the Tension Field a dominant material layer, not a diagram. It should
   occupy a large right-side hero plane on desktop and remain visibly present
   behind/beside the hero content inside the FIRST 390x844 and 360x800 viewport.
   Avoid covering text or hit areas. Remove the visible English internal label
   `TENSION FIELD`.
2. Add believable translucency, edge highlights, subtle optical grain/soft
   shadow and a brighter but restrained inspection-light cut using lightweight
   SVG/CSS only. Preserve warm mineral paper, graphite and amber identity.
3. Connect the signature into the next chapter with one deliberate continuation
   line/layer; eliminate the isolated mobile blank band. Do not rebuild the IA.
4. Refine the display typography and hero composition toward the accepted
   target using local/system font stacks only. Georgia may be a last fallback,
   not the first visual choice. Reduce generic white-form-card residue while
   preserving the exact question-first workflow and 44px targets.
5. Motion must be purposeful and bounded. Remove all decorative infinite loops.
   Use at most: one entrance/inspection sweep and a small pointer or scroll-linked
   response that helps explain film tension/continuity. It must be interruptible,
   passive, transform/opacity/custom-property based, and fully static in
   reduced-motion, Save-Data and no-JS. Never delay content or interaction.

Acceptance intent: signature identity >=8/10, premium craft >=8.2/10, target
translation >=0.72, while product truth/message match remain unchanged.

## Proven functional, accessibility, privacy and truth fixes

6. Add explicit accessible names to the header phone link and all three hero
   channel links so names remain present when visible text is CSS-hidden at
   390/360. Use concise Russian `aria-label` values.
7. Add the established `ym-disable-keys` privacy class to the car model input so
   Webvisor does not capture typed model text. Do not change Metrika code or
   counter configuration.
8. Neutralize unsupported marketing claims everywhere, including metadata/FAQ:
   replace `premium film` with factual `transparent polyurethane film`; replace
   `all remarks are eliminated` with a factual inspection/check statement;
   replace unsupported promises about care guidance and `no hidden stages` with
   neutral process language. Do not invent film brand, thickness, certification,
   outcome, consent or service facts.
9. The proof media publishability audit blocks three current slides. Remove
   `gloss-panel-wide.webp`, `film-edge-process-wide.webp` (identifiable face,
   no recorded consent) and `full-body-disassembly-wide.webp` (missing derivative
   provenance) from this landing. Keep only these three candidate real proof
   slides and correct counters/indices: `finished-porsche-wide.webp`,
   `film-sheet-wide.webp`, `gloss-front-wide.webp`. They remain
   candidate-not-published pending owner rights approval; do not claim otherwise.

## Performance blocker

Independent cold-load QA measured 844.7 KiB initial transfer because every proof
image loads before scroll; budgets are <=350 KiB mobile and <=450 KiB desktop.

10. Keep only the first proof image in initial HTML `src`. Put secondary proof
    sources in `data-src` and hydrate them only when the proof section approaches
    the viewport or when the user explicitly navigates. No broken-image icon,
    no empty essential proof state and no autoplay. With no JS the first proof
    image and all factual proof copy must remain visible and usable; secondary
    controls may remain text/number-only. After deliberate traversal, every
    retained proof image must load/decode and the manual gallery must work.

## Static checks before returning

- `node --check assets/qwen-full-film.js`
- only the three allowed source files changed in this pass
- no infinite CSS animation declarations/iterations
- no removed/blocked proof filenames remain in `index.html`
- all responsive icon-only links have accessible names
- car input contains `ym-disable-keys`
- initial HTML has no eager `src` for secondary proof images
- price, terms, contacts and canonical event names are unchanged

Return a concise exact change list and any browser evidence that still requires
Sol verification. Do not claim owner-grade completion; Sol will rerun independent
visual, performance, accessibility, tracking and provenance gates.
