# Qwen 3.7 Max refinement task: own and rebuild pass 2

## Outcome

Your first implementation is real and structurally different, but it failed the owner-grade gate. You remain the design and implementation lead. Perform a second authorial implementation pass in this same isolated worktree. Do not merely fix tests. Rebuild the visible experience so the selected Qwen "Tension" vision reaches the accepted internal targets while preserving product truth and conversion clarity.

Read completely before editing:

- `docs/qwen-full-film-experiment/SOURCE_OF_TRUTH_ASCII.md`
- `docs/qwen-full-film-experiment/SOL_RED_TEAM_FINDINGS_RU.md`
- `docs/qwen-full-film-experiment/REFERENCE_ATLAS_RU.md`
- `docs/qwen-full-film-experiment/CONCEPT_SCORECARD_RU.md`
- `docs/qwen-full-film-experiment/VISUAL_TARGET_TRANSLATION_CONTRACT_RU.md`
- `artifacts/qwen-full-film-concept-targets/desktop-tension-target-v2.png`
- `artifacts/qwen-full-film-concept-targets/mobile-tension-target-v2.png`

The target images are generated illustrative UI references only. Never embed them in the live page, never call them proof, and never copy their generated English text. Translate the direction into accessible code-native HTML/CSS/SVG/canvas.

## Design correction

- Keep your warm editorial Qwen thesis, but make the product-specific Tension Field large and unmistakable in the desktop first viewport. The transparent layer must follow an abstract body-panel contour with one directed inspection-light band. It must not look like a real customer car, workshop, video or actual mounting equipment.
- On mobile, show a compact static/low-motion version of the field in the first viewport without covering the service, 180,000 RUB or first action.
- Remove the large dead desktop half. Do not solve this by adding cards, a generic car render or random stock.
- Your current narrative still reads as hero -> numbered process cards -> carousel -> package -> contact -> reviews -> FAQ. Restructure it so the Tension Field or one continuous edge/light line connects the product explanation, truthful proof and scope. Avoid another numbered card process as the dominant architecture.
- You may keep the serif thesis, but do not claim Georgia is new relative to baseline. Use a genuinely different, verified-license self-hosted type system only if you can document source/license/weight; otherwise build distinction through composition and type behavior without an unverified download.

## Required technical corrections

Close every SOL-001 through SOL-013 finding in `SOL_RED_TEAM_FINDINGS_RU.md` or mark it honestly blocked with evidence. In particular:

1. Progressive enhancement: content is visible with JavaScript disabled. Motion hiding is enabled only after a safe JS-ready state.
2. Fresh normal-motion QA must scroll through every chapter before full-page screenshots and prove zero hidden reveal nodes at the end.
3. Restore canonical attribution fields `scenario` and `experiment_id`, plus message match for `utm_content=newcar_fullfilm` and `utm_content=price_install_fullfilm`, without changing the verified offer.
4. Every direct channel has an exact contact location; QA triggers and verifies the full canonical event map and counter while producing zero external Metrika requests in QA.
5. Implement real Save-Data/slow-connection behavior. Continuous decorative animation must stop.
6. Make the car input outcome truthful for WhatsApp, Telegram and MAX. If Telegram/MAX require copy-and-paste, explain that before the click and test the copied text. No invented response speed.
7. Remove unverified review themes or map them to a verified source. Neutral direct links are acceptable.
8. Make the proof gallery manual-first or provide an explicit pause control with correct accessibility semantics. Avoid autonomous aria-live churn.
9. Normalize source to UTF-8 without BOM/zero-width markers.
10. Measure Lighthouse or equivalent realistic mobile lab metrics plus full request/transfer inventory. Do not claim INP if it is not measurable in lab.

## QA and proof v2

- Use fresh output folder `artifacts/qwen-full-film-qa-v2/`.
- Required widths: 1440, 430, 390, 360.
- Capture hero viewport, scrolled chapter/proof/contact states, and post-traversal full page for each width.
- Capture reduced-motion, Save-Data and no-JS states.
- Check overflow, broken media, console/page/request failures, focus order, keyboard interaction, tap targets, dialog, manual proof control, contact message behavior, event map, QA isolation and message match.
- Run a direct old-vs-v2 and target-vs-v2 visual self-critique. If the signature is not visible or the result still looks like a safe editorial template, iterate again before returning.

## Workspace hygiene

- Do not commit, push, PR, deploy or touch live systems.
- Do not change `/risk-zones/` or `/color-film/`.
- Do not delete files you did not create. List the exact temporary `scripts/*_part*`, writer scripts and `node_modules` as cleanup candidates; the parent will perform verified cleanup after your run.
- Keep only a minimal project QA dependency in package metadata if justified. Never treat `node_modules` as a deliverable.

## Final response

Return COMPLETE only if pass 2 source is built, v2 proof exists, the signature is visible in desktop/mobile hero, and the blocking findings are closed with evidence. Otherwise return INCOMPLETE and name the exact remaining gate.
